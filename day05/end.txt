1. 使用*binfo来表示这个内存地址上12字节的结构体。这与“char *p“中的*p表示p地址的1字节是同样的道理。
2. 显示字符。字符对应的二进制与字符对应的显示描述矩阵有一一对应关系。
3. 把段描述符分成6个部分看待，4类，初始化时使用3个参数。
4. 关于指针（地址变量）：二进制：0f0116e6c4指令为加载GDT，反汇编：LGDT ds:0xc4e6，nasm汇编需写为：LGDT [0xc4e6], LGDT 0xc4e6是错误的格式。
